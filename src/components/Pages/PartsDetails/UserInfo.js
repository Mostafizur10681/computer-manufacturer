import React from 'react';

const UserInfo = () => {
    const handleSubmit = event => {
        event.preventDefault()
    }
    return (
        <div>

        </div>
    );
};

export default UserInfo;